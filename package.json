{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node -e \"require('node:fs').rmSync('public/hot',{force:true})\" && vite build",
        "dev": "vite",
        "adb": "adb reverse tcp:8000 tcp:8000 && adb reverse tcp:5173 tcp:5173"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "@laravel/passkeys": "^0.2.0"
    }
}
